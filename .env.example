# ============================================================================
# EJEMPLO DE ARCHIVO .ENV PARA REGISTRO DE JORNADA
# ============================================================================
#
# IMPORTANTE: Este es un archivo de ejemplo. NO contiene credenciales reales.
#
# Para configurar el sistema:
# 1. Copie este archivo a .env (en el mismo directorio)
# 2. Ejecute: python scripts/encrypt_secrets.py
# 3. Siga las instrucciones del script para generar valores encriptados
# 4. Pegue los valores generados en su archivo .env
# 5. NUNCA comparta el archivo .env con nadie
#
# ============================================================================

# ----------------------------------------------------------------------------
# CLAVE DE ENCRIPTACIÓN (generada automáticamente por el script)
# ----------------------------------------------------------------------------
# Esta clave se genera al ejecutar scripts/encrypt_secrets.py
# IMPORTANTE: Si pierde esta clave, deberá volver a encriptar todos los secretos
ENCRYPTION_KEY=your_encryption_key_here

# ----------------------------------------------------------------------------
# CONFIGURACIÓN DEL BOT DE TELEGRAM (valores encriptados)
# ----------------------------------------------------------------------------
# Token del bot de Telegram (obténgalo de @BotFather)
BOT_TOKEN_ENCRYPTED=your_encrypted_bot_token_here

# ID del chat de Telegram (obténgalo enviando un mensaje al bot)
CHAT_ID_ENCRYPTED=your_encrypted_chat_id_here

# ----------------------------------------------------------------------------
# CREDENCIALES DEL SISTEMA ViveOrange (valores encriptados)
# ----------------------------------------------------------------------------
# Usuario de ViveOrange
HR_USERNAME_ENCRYPTED=your_encrypted_username_here

# Contraseña de ViveOrange
HR_PASSWORD_ENCRYPTED=your_encrypted_password_here

# Código de empleado
EMPLOYEE_CODE_ENCRYPTED=your_encrypted_employee_code_here

# ============================================================================
# NOTAS DE SEGURIDAD
# ============================================================================
#
# 1. Este archivo (.env) NUNCA debe compartirse ni subirse a control de versiones
# 2. Verifique que .env está en su archivo .gitignore
# 3. Todos los valores deben estar encriptados usando el script proporcionado
# 4. Guarde una copia segura de ENCRYPTION_KEY en un gestor de contraseñas
# 5. Si sospecha que las credenciales están comprometidas:
#    - Cambie inmediatamente las contraseñas en ViveOrange
#    - Revoque y regenere el token del bot de Telegram
#    - Ejecute nuevamente el script de encriptación con nuevos valores
#
# ============================================================================
